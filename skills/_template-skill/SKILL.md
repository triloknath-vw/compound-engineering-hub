---
name: _template-skill
description: 'TEMPLATE — not a real skill. Copy this folder, rename it, update this description with real "Use when..." trigger phrases, then remove the user-invocable/disable-model-invocation lines below.'
user-invocable: false
disable-model-invocation: true
---

# <Skill Name>

<!-- This template is inert (see frontmatter above) until you rename it. -->

## When to Use

- <Trigger scenario one>
- <Trigger scenario two>

## Procedure

1. <Step one>
2. <Step two — reference bundled assets like [a script](./scripts/example.py) using relative paths>
3. <Step three>

## Notes

- Keep `SKILL.md` under ~500 lines; move detail into `references/`.
- Keep resource references one level deep from `SKILL.md`.
